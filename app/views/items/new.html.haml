%h1 Add Image
.row
  .col-md-6.col-md-offset-3
    = form_for(@item) do |f|
      = f.label :image
      = f.file_field :image, class: "form-control"

      = f.submit "Submit",  class: "btn btn-primary"
