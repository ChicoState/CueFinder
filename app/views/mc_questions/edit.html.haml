%h1
  = @question.prompt
.row
  .col-md-6.col-md-offset-3
    = form_for(@question) do |f|
      = f.collection_check_boxes  :mc_choices, @choices, :id, :choice

      = f.submit "Submit",  class: "btn btn-primary"
